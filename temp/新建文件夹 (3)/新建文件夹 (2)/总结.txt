svn版本库中修改文件名称（如大小写）：
先删除之前的文件，提交，再添加新的同名文件，再提交。


java split()分割特殊字符 “|” 时，需要前置两个反斜杠转置：
str.split("\\|")

stringbuffer.append();//拼接字符串
stringbuffer.deleteCharAt(string.length()-1);//删除最后一个字符

string.contains(chrinString);//字符串是否包含子串

jekins部署测试环境时，打开Xshell，部署的同时查看日志进展。
若jekins生成正常，部署失败，查看Xshell的日志，可能加载全局变量问题，复制全局变量链接，在浏览器中访问后，再次部署。



SQL日期错误：
[SQL]SELECT
	*
FROM
	-- 	CLIENTPREFER
	CLIENT
WHERE
	TO_DATE (CORP_END_DATE, 'yyyymmdd') < SYSDATE
OR TO_DATE (CORP_END_DATE, 'yyyymmdd') - SYSDATE <= 15
[Err] ORA-01840: input value not long enough for date format

时间：yyyyMMdd 00-12 时间中分钟恰巧也是00-12 MM月 mm分钟



Python_报错：ValueError: binary mode doesn't take an encoding argument
https://www.cnblogs.com/rychh/p/9833443.html
rb+操作时不支持指定encoding参数


TypeError: a bytes-like object is required, not 'str'
https://www.cnblogs.com/dpf-learn/p/8028121.html





xShell linux指令：
1. cd /app/mom/logs/data-access 切换到工程日志目录
2. tail -f -n200 mom.log  查看日志文件的前200行--查看日志动态增加
3. clear 清除
4. ll（ls -l） vs ls: 都是查看下层目录下含有的文件；ll命令查看的目录按行详细展示
5. cd ..: 回到上一层目录
6. pwd: 显示当前目录
7. ps -ef|grep ServerRun: 查看进程
8. kill xxx: 杀死进程
9. ./进程名字: 启动进程--进入到进程的目录下执行
10. shutdown -h now: 立即关机




CREATE TABLE "APP_COMMON_SERVICE"."PERSON_INFO_MODIFY_LOG" 
   (	"ID" NUMBER(11,0) NOT NULL ENABLE, 
	"SERIAL_NO" VARCHAR2(64) DEFAULT NULL, 
	"CUST_ID" VARCHAR2(64) DEFAULT NULL, 
	"PHONE" VARCHAR2(11) DEFAULT NULL, 
	"MODEL" VARCHAR2(20) NOT NULL ENABLE, 
	"CONTENT" VARCHAR2(2048) NOT NULL ENABLE, 
	"ERROR_LOG" VARCHAR2(2048) DEFAULT NULL, 
	"STATUS" VARCHAR2(11) DEFAULT 0, 
	"VERSION" NUMBER(11,0) DEFAULT 0, 
	"CREATE_TIME" DATE NOT NULL ENABLE, 
	"UPDATE_TIME" DATE DEFAULT NULL, 
	"NOTE1" VARCHAR2(512) DEFAULT NULL, 
	"NOTE2" VARCHAR2(512) DEFAULT NULL, 
	 PRIMARY KEY ("ID")
  USING INDEX PCTFREE 10 INITRANS 2 MAXTRANS 255 
  STORAGE(INITIAL 65536 NEXT 1048576 MINEXTENTS 1 MAXEXTENTS 2147483645
  PCTINCREASE 0 FREELISTS 1 FREELIST GROUPS 1 BUFFER_POOL DEFAULT FLASH_CACHE DEFAULT CELL_FLASH_CACHE DEFAULT)
  TABLESPACE "USERS"  ENABLE
   ) SEGMENT CREATION IMMEDIATE 
  PCTFREE 10 PCTUSED 40 INITRANS 1 MAXTRANS 255 NOCOMPRESS LOGGING
  STORAGE(INITIAL 65536 NEXT 1048576 MINEXTENTS 1 MAXEXTENTS 2147483645
  PCTINCREASE 0 FREELISTS 1 FREELIST GROUPS 1 BUFFER_POOL DEFAULT FLASH_CACHE DEFAULT CELL_FLASH_CACHE DEFAULT)
  TABLESPACE "USERS";

CREATE INDEX "APP_COMMON_SERVICE"."MODIFY_LOG_CUSTID" ON "APP_COMMON_SERVICE"."PERSON_INFO_MODIFY_LOG" ("CUST_ID", "MODEL") 
  PCTFREE 10 INITRANS 2 MAXTRANS 255 COMPUTE STATISTICS 
  STORAGE(INITIAL 65536 NEXT 1048576 MINEXTENTS 1 MAXEXTENTS 2147483645
  PCTINCREASE 0 FREELISTS 1 FREELIST GROUPS 1 BUFFER_POOL DEFAULT FLASH_CACHE DEFAULT CELL_FLASH_CACHE DEFAULT)
  TABLESPACE "USERS";

CREATE INDEX "APP_COMMON_SERVICE"."MODIFY_LOG_PHONE" ON "APP_COMMON_SERVICE"."PERSON_INFO_MODIFY_LOG" ("PHONE", "MODEL") 
  PCTFREE 10 INITRANS 2 MAXTRANS 255 COMPUTE STATISTICS 
  STORAGE(INITIAL 65536 NEXT 1048576 MINEXTENTS 1 MAXEXTENTS 2147483645
  PCTINCREASE 0 FREELISTS 1 FREELIST GROUPS 1 BUFFER_POOL DEFAULT FLASH_CACHE DEFAULT CELL_FLASH_CACHE DEFAULT)
  TABLESPACE "USERS";

CREATE UNIQUE INDEX "APP_COMMON_SERVICE"."SYS_C0035112" ON "APP_COMMON_SERVICE"."PERSON_INFO_MODIFY_LOG" ("ID") 
  PCTFREE 10 INITRANS 2 MAXTRANS 255 
  STORAGE(INITIAL 65536 NEXT 1048576 MINEXTENTS 1 MAXEXTENTS 2147483645
  PCTINCREASE 0 FREELISTS 1 FREELIST GROUPS 1 BUFFER_POOL DEFAULT FLASH_CACHE DEFAULT CELL_FLASH_CACHE DEFAULT)
  TABLESPACE "USERS";

新建表时，唯一性主键系统自动生成（SYS_C0035112）。不需要额外添加。（CREATE UNIQUE INDEX "APP_COMMON_SERVICE"."SYS_C0035112" ）




1.记一次开发环境的bug，dubbo调试跑到别的机器上。在dubbo注册中心查看消费者与生产者；
2.Autowired注解，实例化service对象；
3.catch中包含可能抛异常的内容，再包一层try-catch；
4.Dao中insert，delete等无返回，再impl操作调用时使用try-catch；
5.流水号生成：GUID.GenerteId()；
6.orcle BLOB, CLOB
7.版本号不一致，导致jekins上面发版不成功，找不到对应的新的方法。
8.直接调用service在dao之上在封装一层，尽量不直接在handler中调用dao.
9.指定数据源，在impl或dao中指定数据源@DataSource(target = "ACCOUNT_DATASOURCE")
  impl:
    @Override
    @DataSource(target = "ACCOUNT_DATASOURCE")
    public List<ScoreTaskInfo> selectByParams(ScoreTaskInfoCriteria scoreTaskInfoCriteria) {
        return scoreTaskInfoDao.selectByParams(scoreTaskInfoCriteria);
    }
  dao:
    @DataSource(target = "ACCOUNT_DATASOURCE")
    @Select("SELECT TEBON_ID,CUST_ID FROM CUSTOMER_BASIC_INFO WHERE TEBON_ID IS NOT NULL AND CUST_ID IS NOT NULL")
    List<CustomerBasicInfo> queryClientIdAndTebonId();
10.@DataSource(target = "ACCOUNT_DATASOURCE")中的target，在配置文件（application-context.xml）中指定数据源配置。
11.
batch读取配置中心路径
修改c盘的sys-config_backup.properties文件


从K最近邻算法的角度来看，就是让目标样本与其他正样本距离更近、与其他负样本距离更远，从而使得其近邻中的正样本比例更高，更大概率被判断成正样本。

朴素贝叶斯算法成立的一个前提是满足特征间条件独立假设。

在决策树中，最能区分类别的特征将作为最先判断的条件，然后依次向下判断各个次优特征。决策树的核心就在于如何选取每个节点的最优判断条件，也即特征选择的过程。

我们称逻辑回归为一种线性分类器，其特征就在于自变量x和因变量y之间存在类似y=ax+b的一阶的、线性的关系。对于最终得分y，逻辑回归还通过Sigmoid函数将其变换到0-1之间，其含义可以认为是当前样本属于正样本的概率，即得分y越高，属于“表现好”的概率就越大。

支持向量机致力于在正负样本的边界上找到一条分割界线（超平面），使得它能完全区分两类样本的同时，保证划分出的间隔尽量的大。如果一条分割界线无法完全区分（线性不可分），要么加上松弛变量进行适当的容忍，要么通过核函数对样本进行空间上的映射后再进行划分。

yield from






//新建用户、密码
create user user_name identified by "user_password"
//更新用户密码
alter user user_name identified by "user_password"
//查看当前用户
select distinct owner from all_objects

解锁：
cmd-sqlplus
用户名：system
密码：安装Oracle时输入的管理口令
alter user scott account unlock;








笔记：
1.mybatis 动态sql之if条件判断
    <select id="queryAllByListedNameAndCode" resultType="com.tebon.common.base.vo.KcbInfoAndListedInfoVo">
        SELECT
        LISTED_COMPANY_NAME,LISTED_COMPANY_CODE,COUNT(KCB_COMPANY_NAME) AS LISTED_COMPANY_COUNT
        FROM
        KCB_INFO_AND_LISTED_INFO
        WHERE 1=1
        <if test="listedCompanyName != '' and listedCompanyName != null">
            AND LISTED_COMPANY_NAME = #{listedCompanyName,jdbcType=VARCHAR}
        </if>
        <if test="listedCompanyCode != '' and listedCompanyCode != null">
            AND LISTED_COMPANY_CODE = #{listedCompanyCode,jdbcType=VARCHAR}
        </if>
        GROUP BY LISTED_COMPANY_NAME,LISTED_COMPANY_CODE
    </select>

    WHERE 1=1；
    and写在<if>中间；
    if中判断参数是否为空或空字符串；
    listedCompanyName对应在dao中，参数@Param("listedCompanyCode")设置；

2.expected at least 1 bean which qualifies as autowire candidate for this dependency.
新建的dao无法注入bean。
在dao上新加声明@Repository注解。
@Repository
public interface KcbInfoAndListedInfoDao {
    List<KcbInfoAndListedInfoVo> queryAllByListedNameAndCode(String name,String code);
}

3.date format时间
数据库中定义字段为date，读取后：
Date date = listedInfoVo.getKcbCompanyApplyTime();
格式化：
s = DateFormat.getDateInstance().format(date);	//输出格式为2006-4-16，只有年月日，无时间

4.python遍历字典：
for key,value in dict.items():
	pass

5.字典中相同的key，合并value：
for key,value in dict.items():
	if one in value:
        temp = dic.get(one)
        if temp:
            dic[one]=temp+","+key	//拼接相同key的value
        else:
            dic[one]=key

6.GROUP BY我们可以先从字面上来理解，GROUP表示分组，BY后面写字段名，就表示根据哪个字段进行分组，如果有用Excel比较多的话，GROUP BY比较类似Excel里面的透视表。
GROUP BY必须得配合聚合函数来用，分组之后你可以计数（COUNT），求和（SUM），求平均数（AVG）等。
对于选择的字段都要写在GROUP BY中。

7.Python代码编码问题：SyntaxError: Non-UTF-8 code starting with '\xe4'
错误原因： 
导致错误的原因是程序中插入中文后的编码问题。
解决方法： 
在程序的开始加入下面的一句语句：
# coding = utf-8
再次运行程序，便可以正常运行了。

8.Mybatis like 模糊查询问题
在Mybatis的xml中使用like时，报错：无效的列索引，解决方法如下：
like '%'||#{参数}||'%'
或
like concat(concat('%',#{参数}),'%')

9.sql中||连接符
||表示连接，如'a'||'b'等价于'ab'

10.remove()循环删除
# 移除港股
for col in colRe[:]:	# 遍历复制的序列
    if '（港）' in col:
        colRe.remove(col)

11.选择某行元素
核心思想：依据行号选择
improt pandas as pd
df=pd.readExcel()
r1 = df[0:1]  #根据行号索引选择 选择第0行
选择某列：df['a']  #直接选取列名

12.获取dataframe的行数和列数
df.shape[1]	#返回列数
df.shape[0] #返回行数 or len(df)

13.for循环里添加两个变量
控制条件连接为 && ；
for(int i=0,j=0; i<I.length() && j<J.length(); i++,j++){
	//
}

14.python json和字符串互转
json->str: json.dumps(json)	# json转为dict  resultJson = {"state":1}
str->json: json.loads(string)










