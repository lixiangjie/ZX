1.背景知识
	此法，句法，语义分析：消除歧义问题。
	NLP应用的歧义：同音不同意；

2.实例讲解
	新词发现算法；添加自定义词；
	凝聚度=互信息，及同时出现的概率。
	自由度=左右信息熵，即左右搭配的丰富程度。左右搭配的可能性越多，自由度越高。
	CRF；
	one-hot：维度过长，多数为空；编码；出现为1，超长维度编码；
	word2vec：周围词预测接下来的词；向量包含上下文语义；（如何保证准确性与学习效率？反馈？）
	squs2sque：

3.应用场景
	信息抽取。
	基金的知识库：RDF形式，存储成mongodb；图数据库存储；
	SPARQL查询语言，直接翻译与理解用户意图，返回答案；
	文本表示：bag-of-words；主题模型；基于词向量的固定表征；基于词向量的动态表征；
	归一化函数；统一结果；
	CNN模型；
	文本分类：字符文本在模型中更加抽象，不可解释性，与图像模型不同。
	文本摘要：技术框架：内容描述；权重计算；内容选择；内容组织。
	textRank算法--pageRank算法（引用权重，点击权重；），有向图，构造矩阵连接关系，计算两个句子的相似度，所有图的边计算相似性。
	文本标签：需要有一定的文档覆盖率，一边对类似文档按标签进行分类显示。
	TF-IDF算法：统计模型，统计量
	词频（TF）=某个词在文章中的出现次数；
	词频（TF）=某个词在文章中的出现次数/文章的总词数
	逆文档频率（IDF）=log(语料库的文档总数/(包含该词的文档数+1))
	TF-IDF=词频（TF）*逆文档频率（IDF）

	textRank：关键词发现；


通信，统计基础；

敏感词库，可维护；相似度对比，过滤；可控；

